# Docs

The index: path plus one line per page. Read this first, then read only the
pages the task needs.

- `greeting.md` — how a greeting is built, and the parts of the day
- `index.md` — this page
