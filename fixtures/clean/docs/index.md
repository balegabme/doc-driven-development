# Docs

The index for the clean fixture. It carries no `Files:` section and is passed
to the checker as an exemption.

- `area.md` — the line-comment and block-comment headers
- `nested/extra.md` — a page in a subdirectory
