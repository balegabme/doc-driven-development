# Extra

Files:
- src/hash.py — a `#` header
- src/widget.svelte — an HTML comment header

This page sits in a subdirectory, so a header pointing at it only resolves when
the doc directory is scanned recursively.
